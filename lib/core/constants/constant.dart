import 'package:flutter/material.dart';

class AppConst {
  // 🎨 Couleur violette d’arrière-plan (haut de la page)
  static const Color purpleBackground = Color(0xFFC8A2F2);

  // 🟣 Couleur violette du bouton
  static const Color purpleButton = Color(0xFFA582E5);

  // 💜 Violet foncé (icônes, accents)
  static const Color purpleDark = Color(0xFF7042C9);

  // 🌫️ Violet très clair (fond des champs de saisie)
  static const Color purpleInputFill = Color(0xFFF1EFFE);

  // ⚫ Couleur du texte principal
  static const Color textDark = Color(0xFF1F1F2E);

  // ⚪ Couleur du texte secondaire (contraste ≥ 4.5:1 sur blanc et purpleInputFill)
  static const Color textGrey = Color(0xFF5F5F72);

  // ✅ Vert de succès (bonnes réponses, validations)
  static const Color successGreen = Color(0xFF32C832);

  // 🔤 Police principale
  static const String fontFamily = 'Roboto';
}
